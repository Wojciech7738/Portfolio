# PDF rotator

This is a simple application for "fixing" a PDF documents whose pages are placed unproperly.

To run this program execute in console "python rotv2.py". Then select a PDF file and choose
the direction (angle) of the rotation (90° by default, counterclockwise). Finally, press "Rotate" button.
